# Vibevilla

This repository contains the `backend/` and `frontend/` folders for the Vibevilla project.

## Structure

- `backend/` — server-side code
- `frontend/` — client-side code

## Next steps

1. Add your backend source files into `backend/`
2. Add your frontend source files into `frontend/`
3. Commit and push changes
